import 'package:flutter/material.dart';

class RasedPage extends StatelessWidget {
  const RasedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text("Rased Screen"),
        ),
      ),
    );
  }
}
